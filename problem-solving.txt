This is my first problem solving plan and I will highlight what are the premises for this project.


Divide problems and solve it

This section is for dividing the problems encountered by me in this project. 
The core idea is that dividing a big problem to smaller problem will help me solve a problem easily. 
This can be done solving smaller problems and connecting all the solutions to arrive at the desired output.

1. First, create a function where the computer can return rock, paper or scissor randomly.
 - Math.random() solves the issue of returning random number between 0 (included) and 1 (excluded).
 - multiplication with 100 gives an idea between 100 and 0.
 - For perfectly obtaining the three variables randomly with equal probability, separate number evenly so the result is equal to randomness.

2. Second, create a function that takes human value as input and stores it and returns it.

3. Now, the game need to store both human and computer score(i.e won by computer or human).
 - create two variables that will store the score in global scope(i.e the scores are stored outside of functions).
 - to do that the variables are first initialized with number 0.

4. Now, i need to create a round to play.
 - First, code a new function to take both player and computer choices.
 - give two parameters to that function called computerChoice and humanChoice.
 - now arguments can be passed to that function playRound through computerChoice and humanChoice.
 - for first argument computerChoice the function getComputerChoice value will be passed through.
 - for second argument humanChoice the function getHumanChoice value will be passed through.
 - so, i need to pass two arguments to the playRound function parameter.
 - when a player wins the round their score is incremented and added to the variable computerScore or humanScore depending on the winner.
 - Increment the score inside playRound itself.
 - There are two scenario one is when computer win and another is the user win.
 - i need to find a way to increase a user score when they win, how can it be done?
 - So it can be done to see if a string contains you win and you lose, if win the score is added if lose not add any score same for tie.
 - log it into a score chart that determines what score it is based on the winner and loser.

5. Final part is to create a new function called playGame that will call playRound 5 times an keeps track of the score and give result based on the score.
 - Create a new function called playGame.
 - Call the function playRound 5 times from playGame.
 - Record the scores, if 5 rounds are complete then determine winner based on the highest number of wins in a game(i.e five rounds).
 - do code to determine how winner can be declared using score.
  - After five rounds write if code to determine who is winner by comparison operator.
  - if one is higher than the other then declare them winner if they get tied then tell it is tied.
  - This score comparison only happens after the 5 rounds not in middle.
  - The ultimate winner is decided by an if....else condition.
